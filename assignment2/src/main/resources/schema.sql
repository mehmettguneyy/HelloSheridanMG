CREATE TABLE pet (
                         id INT AUTO_INCREMENT PRIMARY KEY,
                         pet_name VARCHAR(30),
                         kind_name VARCHAR(30),
                         pet_gender INTEGER,
                         pet_vac BOOLEAN

);